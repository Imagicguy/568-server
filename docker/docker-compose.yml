version: '2'

services:
  proxy:
    user: root
    build: ./proxy
    volumes:                
      - ./proxy:/code       
      - ./logs:/var/log/erss
    command: bash -c "./proxy"
    ports:
      - "12345:12345"